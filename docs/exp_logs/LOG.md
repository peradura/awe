# 실험 로그 인덱스

자동/수동 기록: 실행 로그는 `results/`, 아래 표는 인용할 만한 실험의 한 줄 인덱스.
형식·규칙은 [`LOGGING.md`](../../LOGGING.md) 참고. (판정: ✅ positive / 🟡 directional / 🔴 negative)

| 날짜 | 실험 (module) | 컨셉 | 결과 | 산출물 |
|------------|---------------|------|------|--------|
| 2026-07-03 | `depth_sanity` | in-context reachability로 **깊이 손잡이** sanity | ✅ 깊이↑→정확도↑(r1=64%→r6=100%), `corr(K, 수렴스텝)=+0.92` — halting=사후 예측-안정성(수렴), surprise 아님 | — (로그 미보존 — 재생성 필요) |
| 2026-07-03 | `ablation_ttt` | **공유 신호**(recon error가 양쪽 구동) fast-weight 모델 + 4-way ablation (reachability) | 🟡 배관 검증(과제가 너무 쉬워 config 구분 약함) — **논지 그대로의 구성은 이 실험뿐, 미결** | (미보존 — `accuracy_vs_steps.png` 커밋 안 됨) |
| 2026-07-06 | `ablation_amort` | full-table reachability에 query 스트림+persist 메모리 | 🔴 **negative** — 전부 100%·amortization 평평(그래프가 context에 있어 메모리 잉여). 주의: 단일 tau 공유 → `+halt` 평평함은 일부 tau-스케일 아티팩트 | (로그 미보존) |
| 2026-07-06 | `ablation_rule` | **hidden permutation + 부분관측** 스트림 (메모리 손잡이) | ✅ **memory-knob positive** — capability gap persist 5%→81%(both 5→79%), amortization 8→1.2스텝, `corr(ans, 엔트로피)=−0.96`, both=persist 정확도를 2.4 vs 8스텝. **halting=엔트로피, 쓰기=recon error(두 신호)** | `results/rule_curve.png`, `results/hidden_rule_run.log` |
| 2026-07-06 | `ablation_reachp` | **부분관측 reachability** — 깊이+메모리 결합 stress-test | 🔴 **joint negative / 🟡 memory-only** — persist 35.3%(22%→41%)인데 **both 25.7%(−9.6pp): halting을 켜면 손해**; +halt 16.0% < fixed 21.0%; K≥2 예산 포화. `corr=−0.23`은 라벨링 아티팩트로 과소평가(아래 행) | `results/reachp_curve.png`, `results/reachp_run.log` |
| 2026-07-06 | `reachp.py` 라벨링 수정 + 재실행 | `ans`가 현재 쿼리 공개분을 무시(모델은 retrieval 전에 write) → 라벨 교정 후 동일 시드 재실행 | ✅ 학습·정확도 완전 재현(loss·acc 동일), **corr −0.229 → −0.293** — 아티팩트는 약한 결합의 일부만 설명; joint 과제의 약한 신호-답변가능성 결합은 실재 → bake-off 필요성 강화 | `results/reachp_run_v2.log`, `results/reachp_curve_v2.png` |
| 2026-07-06 | 문서 재캘리브레이션 (외부 리뷰) | 신호 인벤토리 명시(엔트로피 vs recon error), Part 3 both-vs-persist 손실 명시, UT-Memory/HRM/TRM 인용, C4 철회, 한계 섹션 추가 | — | `docs/RESULTS.md` "Signal inventory", `docs/REVIEW.md`, `docs/proposal.md` 부록 |
| 2026-07-06 | `ablation_reachp2` | 개선판: K-curriculum + aux next-node loss + d=256 (GPU0, 8k) | 🟡 **부분 성공** — curriculum+aux가 base-learner 병목 해소: persist 35→**72%**, amortization 40→**79%**. 그러나 **halting을 켜면 여전히 정확도 손실**(both 44.6% < persist 72.2%), depth∝K 조잡(K≥1 증가하나 예산 포화, K=0 이상치). ans-fix 재실행(v2, 동일시드): 학습·정확도 완전 재현, **corr −0.33→−0.44** (아티팩트 제거 후에도 hidden-rule −0.96 대비 약함 = joint의 약한 결합 실재). 진단 확정: **base-learner를 고쳐도 halting-신호가 병목** → bake-off로 직행 | `results/reachp2_curve.png`, `results/reachp2_run.log`, v2: `results/reachp2_v2_*` |
| 2026-07-06 | `ablation_reachp3` | **halting-신호 bake-off** (entropy/conv/dstate/recon, held-out tau, **10 seeds**, 실패분해) | ✅ **판정(10시드 재현)** — persist 천장 72.0±0.6%. **conv·dstate가 halting 비용 제거**(gap −0.0pp): **conv** 5.00±0.58스텝(**bimodal**: 7/10 시드만 ~4.6스텝·early-right~46%로 절감, 3/10은 dstate처럼 ~5.9스텝); **dstate**(=‖Δs‖²) 5.93±0.06스텝·early-wrong 1.1%로 가장 안전하나 **보수적**(거의 halt 안 함). **정확도 −0.0pp는 전 시드 견고, compute 절감은 seed-fragile**. (aggregate 로그의 'best arm=dstate' argmax는 노이즈 내 — conv/dstate 정확도 동률.) **entropy·recon −5.6pp 확정 패배**(early-wrong 8–9%, corr(ans) −0.45/−0.25) → 기존 halting 손실은 **신호 선택 오류**. 단 **compute-효율 최선(conv)은 write측 신호 아님** → 강형("한 스칼라가 양쪽 최적 구동")은 미지지; 깊이·write는 서로 다른 observable(두 gradient 통합은 미검정 해석) | `results/reachp3_seed{0..9}.json`, `results/reachp3_bakeoff.png`, run logs |
| 2026-07-06 | `bakeoff` (rule/reachp, seeds 0–9) — 병렬 세션 | 원본 과제(**약한 learner**)에서 독립 bake-off: ent/recon/rnorm/dstate/dent + shuf_g/**shuf_b**(블록내 셔플) 대조 + **tau-스윕 곡선** + 실패분해, held-out tau(**slack-내-최소스텝** 규칙) | ✅ **reachp3와 독립 수렴** — rule(persist 50.5±0.4): **recon 50.1±0.5@2.38 ≈ ent 50.1±0.7@2.22**(메모리-단독 과제에선 recon-halting 생존 — 단 이 recon은 **decodability proxy**로 reachp3의 state-mismatch recon과 **다른 스칼라**), dstate 최선(50.3±0.6@**1.90**). reachp(persist 35.1±0.7): **dstate만 유용 작동점**(33.8±0.6@6.28, −1.3pp·컴퓨트 63%), ent는 halting 포기(9.71/10), recon −3.4pp(tau FALLBACK). 기존 −9.5pp "halting 손해"는 **median-tau 아티팩트가 상당분** | `experiments/bakeoff.py`, `results/{rule,reachp,bakeoff_*}_s[0-9].*`, `results/sweep{59,34}_console.log` |
| 2026-07-06 | held-out tau + 인프라 — 병렬 세션 | ① `calib_tau` held-out 배치화(rule/reachp/reachp2), ② bake-off 스크립트(tau-스윕·ckpt 재사용·shuf_b·실패분해), ③ 스윕·집계·공유GPU 예절 러너 | ✅ 완료 | `experiments/bakeoff.py`, `scripts/{sweep.sh,aggregate.py,gpu_watch_run.sh}`, `docs/RUNBOOK.md` |
| 2026-07-06 | `ablation_reachp2` held-out tau 재현 — 병렬 세션 | 교정 프로토콜 완전판(ans-fix + held-out tau)으로 3차 독립 재현 (CPU, seed 0) | ✅ **재현 일치** — persist 72.0%(GPU판 72.2%), both 44.6%(동일), **corr −0.442**(v2의 −0.44와 일치). held-out tau로 바꿔도 median-tau 대비 유의차 없음 → reachp2 수치는 프로토콜에 강건 | `results/reachp2_run_heldout.log` |
| 2026-07-06 | `ablation_reachp2` 다시드 (10 seeds) | curriculum+aux base-learner fix의 견고성 + amortization/depth 오차막대 (JSON+aggregate 추가) | ✅ **재현** — persist **72.1±0.6%**(메모리 필수: reset 43.3±0.3% → persist 72.1%), amortization q0→q11 43→77%, corr(ans,sur0) −0.452±0.010. depth∝K: K1 2.8→K4 5.95(단조, 상한 포화). `both`(entropy·median-tau) 44.9±0.4%는 **tau 미최적화 아티팩트**(bake-off의 held-out-tau가 초과설명) — halting 비용 재확인 아님 | `results/reachp2_seed{0..9}.json`, `results/reachp2_agg_curve.png` |
| 2026-07-07 | **정준 bake-off** `bakeoff --task reachp2` (10 seeds, 양 GPU) | 화해 후속: B의 엄격 프로토콜(slack-tau·shuf_b 널·tau-스윕)에 **conv 추가** + **강한 학습기**(reachp2, ckpt 재사용)로 6신호 정준 비교 | ✅ **bimodality = tau-규칙 아티팩트 확정·해소** — 천장 71.9±0.5%. **conv 71.2±0.5%@3.89±0.09스텝(35% 절감, per-seed 3.84–4.07 완전 균일)**, dstate 71.0±0.6%@5.12±0.15(보수성도 완화). **수렴군만 slack-내 작동점 존재(tau_ok 10/10), ent/recon/rnorm/dent 전부 0/10 FALLBACK(−3~−6pp)** → 판별이 이항(작동점 유무)으로 더 날카로워짐. shuf_b 널 대비 conv +7.4pp(진짜 per-example 정보). premature/halt: conv 0.02. 이 프로토콜(bakeoff.py+강학습기)이 **정준**; reachp3는 역사 기록으로 은퇴. 부수: bakeoff.py CUDA device 버그 수정(quantile linspace) | `results/bakeoff_reachp2_s{0..9}.{json,log}`, `results/ckpt_reachp2_s*.pt`(로컬) |
| 2026-07-07 | `ablation_mqar` (10 seeds, GPU1) | **외부과제 전이** — single-hop MQAR(vocab 64, KV 4/query)로 수렴-halting bake-off | ✅ **전이 확인 / 🟡 판별력 없음** — persist 68.2±0.5%(~30%는 unanswerable 미보유키). **conv 무손실·2.53스텝(≈58% 절감)**로 전이 성공(reachp 5.0보다 큼, bimodal 없음). 단 **4개 신호 전부 무손실**(entropy·recon도 −0pp): MQAR 오답=unanswerable(어떤 depth로도 오답)이라 조기 halt 무해 → conv의 early-wrong 32%는 "못 푸는 걸 빨리 포기". reachp의 confident-wrong-multi-hop 실패모드 부재. depth∝difficulty 없음(load↑→halt-step 4.8→3.3 = amortization). → 판별 재현엔 **난이도↑ 버전 필요(queued, GPU 완전 유휴 시)** | `results/mqar_seed{0..9}.json`, `results/mqar_bakeoff.png` |
| 2026-07-07 | `ablation_mqar_hop` (10 seeds, GPU1) | **multi-hop MQAR**(H-hop 합성 g^H(q), H∈1..3=난이도) — 판별 버전 | ✅ **판별력 재현 + depth∝난이도** — persist 41.6±0.9%(fixed-depth 정점 depth3 41.9%→6 41.6% 드리프트). **conv/dstate가 entropy/recon를 +2.5pp·전 시드(10/10) 이기고 컴퓨트 절반**(2.5 vs 5.4스텝): conv−entropy +2.76±0.35pp. 메커니즘은 reachp와 다름(여기선 entropy/recon이 수렴 못 잡고 budget까지 드리프트 −2pp, conv/dstate는 정점서 멈춰 +0.6pp) but 결론 동일(수렴>entropy/recon). depth∝H: dstate halt-step H1 2.75→H3 3.51. caveat: base learner 약함(41.6%). → **reachp 판별이 외부 associative-recall로 일반화** | `results/mqarhop_seed{0..9}.json`, `results/mqarhop_bakeoff.png` |


## 메모
- **핵심 전환점**: `ablation_amort`(negative) → 진단(그래프가 context에 있어 메모리 잉여, shortcut 없음) → `ablation_rule`(부분관측으로 메모리 필수화) = memory-knob positive. 단, 메모리가 필수인 과제에서 persist가 reset을 이기는 것은 상당 부분 과제 설계의 귀결 — 검증된 것은 하네스+배관.
- **깊이(1) · 메모리(2)는 각자 다른 신호로 깨끗, 결합(3)은 negative** — 원인은 세 갈래로 교란: (a) base-learner의 memory-chain-following 병목, (b) `ans` 라벨링 아티팩트(수정됨), (c) 엔트로피-임계값 halting의 multi-hop 실패(확신-오답 조기 종료 = both가 persist보다 −9.6pp). 이전의 "mechanism 문제 아님" 단정은 근거 부족이라 철회.
- **reachp2가 (a)와 (c)를 분리 확정**: curriculum+aux로 base-learner를 크게 개선(persist 35→72%)해도 **entropy-halting을 켜면 여전히 손해** → 남은 병목은 (c) halting-신호로 좁혀졌고, bake-off가 **(c)는 신호 *선택* 문제**임을 확정(아래).
- **bake-off 판정 (2026-07-06, 10 seeds) — 논지 검정 완료**: 논지 그대로의 **recon 신호는 halting에서 −5.6pp로 패배**(entropy와 동급, early-wrong 9%) → "recon 하나가 양쪽 구동"은 **halting 축에서 반증**. 반면 **conv·dstate(수렴군)는 무비용**. 즉 이전의 −9.6pp/−5.6pp halting 손실은 mechanism 실패가 아니라 **entropy/recon이라는 나쁜 신호 선택** 탓. **정직한 결론**: 강한 통합("한 스칼라가 양쪽 *최적* 구동")은 죽음 — compute-효율 최선은 conv(readout 수렴)인데 write측 역할이 없고, dstate(≈‖∇_s L‖)는 정확도는 보존하나 *보수적*(5.93/6스텝, 거의 halt 안 함)이라 "무비용"은 compute 절감이 아니라 정확도 보존을 뜻함. → **깊이와 write는 서로 다른 observable을 원함**(깊이=수렴, write=recon-miss). "두 gradient=통합"은 거의 동어반복적 *해석*일 뿐, dstate가 write 크기를 예측함을 실증해야 비자명(미검정 — MQAR probe, `docs/mqar_design.md`).
- **두 bake-off는 같은 실험이 아님 (2026-07-07 화해 판정 — 인용 시 주의)**: `ablation_reachp3`(강한 learner 72%, argmax-정확도 tau, recon=state-mismatch)와 `bakeoff.py`(약한 learner, **slack-내-최소스텝 tau**, recon=**decodability proxy**)는 tau-선택 목적함수와 recon 정의가 다르다. ① reachp3의 conv bimodality·dstate 보수성 → **정준 런이 tau-규칙 아티팩트로 확정**(아래 행), ② "recon이 rule에선 생존, joint에선 패배"는 **한 신호의 모순이 아니라 두 정의의 결과**다. 두 표를 한 비교표로 합치지 말 것.
- **다음 (2026-07-07 정준 런 완료 후)**: ① ~~정준 bake-off~~ ✅ 완료(위 행 — bimodality 해소, reachp3 은퇴). ② **write-크기 상관 probe**(비-동어반복 통합 검정: halt 신호가 ‖Δdelta‖·메모리손실 감소를 예측하는가 — `docs/mqar_design.md`). ③ multi-hop MQAR base learner 강화 + 공개 베이스라인 앵커링. GD-step-by-construction은 **후순위 fallback**("위험한 예측"을 걸 때만). in-context regression은 후속 mechanistic probe. 중단 기준 PROJECT.md §7.
